name: Go
on:
  push:
jobs:
  go:
    name: Go
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.17
      - name: Check Go formatting
        run: |
          go fmt ./...
          git diff --exit-code
      - name: Run Go tests
        run: go test -v ./...
